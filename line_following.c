#include "D_Main.h"
#include "D_I2C.h"
#include "D_SIO.h"
#include "D_EIO.h"

void user_main(void) {
    int current_front_sensor_values[3] = {};
    int current_back_sensor_values[2] = {};

    while(TRUE) {
        for (int i = 0; i < 3; i++) {
            current_front_sensor_values[i]
        }
        
    }
}